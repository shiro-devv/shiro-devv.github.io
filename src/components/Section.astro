---
const { className, title } = Astro.props;
---

<section class:list={["panel flex flex-col gap-4 print:gap-2", className]}>
  {title && <h3 class="section-title print:text-lg">{title}</h3>}
  <slot />
</section>
